{
  "name": "ball-bouncing-off-walls",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "beefy src/index.js:dist/bundle.js --live -d",
    "bundle": "browserify src/index.js -o dist/bundle.js"
  },
  "author": "@zealoushacker",
  "license": "ISC",
  "devDependencies": {
    "beefy": "^2.1.2",
    "browserify": "^8.1.1"
  }
}
